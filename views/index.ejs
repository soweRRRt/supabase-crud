<%- include('partials/header', { title: "Главная", user }) %>

<div class="container text-center mt-5">
  <h1 class="display-5 fw-bold mb-3">Добро пожаловать в <span class="text-primary">Supabase CRUD</span></h1>
  <p class="lead text-muted mb-4">
    Это приложение для работы с базой данных Supabase: создание, чтение, редактирование и удаление клиентов.
  </p>

  <% if (!user) { %>
    <a href="/login" class="btn btn-primary btn-lg me-2">Войти</a>
    <a href="/register" class="btn btn-outline-secondary btn-lg">Регистрация</a>
  <% } else { %>
    <a href="/clients" class="btn btn-success btn-lg">Перейти к клиентам</a>
  <% } %>
</div>

<%- include('partials/footer') %>
